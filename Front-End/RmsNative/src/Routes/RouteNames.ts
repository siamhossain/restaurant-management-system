import {PRIVATE_ROUTES} from "./PrivateRouteNames";
import {GLOBAL_ROUTES} from "./GlobalRouteNames";
import {PUBLIC_ROUTES} from "./PublicRouteNames";

const RouteNames = {
    GLOBAL: GLOBAL_ROUTES,
    PUBLIC: PUBLIC_ROUTES,
    PRIVATE: PRIVATE_ROUTES,
};

export {RouteNames as ROUTES};
