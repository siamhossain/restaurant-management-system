//import {prepareStoreContainerObject} from "@/App/Services/Providers/Core/WaterTank/Debugger";

/**
 * @param updateState {any}
 * @param currentState {any}
 */
export const getDebuggableStores = (updateState: any, currentState: any) => ([
    //Register the stores like the following syntax
    //prepareStoreContainerObject(AuthStore, updateState, currentState),
]);
